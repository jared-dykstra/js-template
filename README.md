# Simple bootstrap project for a Javascript library

## Usage

1. `yarn install` to install dependencies
1. `yarn run test` to run mocha tests
1. `yarn start` to run package

Use VS Code and install recommended extensions
